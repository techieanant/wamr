# WAMR Environment Configuration
# 
# For LOCAL DEVELOPMENT:
#   Copy this to .env.local and customize
#
# For PRODUCTION (Docker):
#   Copy this to .env.prod and customize with secure values
#
# See ENVIRONMENT.md for detailed configuration guide

# Environment
NODE_ENV=development

# Server Ports
PORT=4000                    # Backend API port (use 9000 for production)
VITE_PORT=3000              # Frontend dev server port (dev only)
VITE_BACKEND_PORT=4000      # Backend port for Vite proxy (dev only)

# Database
DATABASE_PATH=./data/wamr.db

# Security - REQUIRED! Generate new values!
# Generate JWT_SECRET: openssl rand -base64 32
JWT_SECRET=your-generated-jwt-secret-here

# Generate ENCRYPTION_KEY: openssl rand -hex 32
ENCRYPTION_KEY=your-generated-encryption-key-here

# Admin User
ADMIN_USERNAME=admin@wamr.local
ADMIN_PASSWORD=changeme123456

# CORS (only needed for development)
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_MAX=5

# WhatsApp
WHATSAPP_SESSION_PATH=./.baileys_auth

# Media Monitoring Interval (ms)
MEDIA_MONITORING_INTERVAL_MS=300000

# Logging
LOG_LEVEL=info
LOG_PRETTY=false
